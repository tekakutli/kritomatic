category: layer
command: create-transform
help: Create a transform mask on a layer
args:
- name: layer_name
  type: str
  required: true
  help: Target layer name
- name: --mask-name
  type: str
  default: Transform Mask
  help: Name for the transform mask
action: create_transform_mask
